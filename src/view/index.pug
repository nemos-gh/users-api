<!DOCTYPE html>
html(lang="en")

  include includes/head

  body

    #loader
    #app
      h1 Random Users Api
      ul#users
      button.more-users(type="button") More Users

    script(src="js/bundle.js")